export const phoneRegex = new RegExp(/^\+7\s\d{3}\s\d{3}-\d{2}-\d{2}$/);
export const zipcodeRegex = new RegExp(/^\d+-?\d*$/);
export const noLettersRegex = new RegExp(/^[^A-Z]*$/gi);
export const anySybmolRegex = new RegExp(/\S*/g);
export const filterValueRegex = new RegExp(/[_()+.-]/g);
